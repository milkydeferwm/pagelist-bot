[package]
name = "pagelist-bot"
version = "0.0.1"
edition = "2021"

build = "build.rs"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
mediawiki = "^0.2"
lalrpop-util = "^0.19"
regex = "1"

[build-dependencies]
lalrpop = "^0.19"
